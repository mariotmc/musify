<h2>Pick a song</h2>

<%= turbo_frame_tag "song_search_query", class: "w-full" do %>
	<%= form_with url: song_searches_path, method: :get, data: { turbo_stream: true, controller: "guess-form" } do |f| %>
		<%= f.hidden_field :round_id, round.id %>
		<%= f.hidden_field :player_id, player.id %>
		<%= f.text_field :query, params[:query], data: { guess_form_target: "input" }, autofocus: true, placeholder: "Search for a song..." %>
	<% end %>

	<%= turbo_frame_tag "song_search_results", class: "w-full" %>
<% end %>
