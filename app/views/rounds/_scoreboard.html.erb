<%= turbo_frame_tag "standings" do %>
	<h2>Standings</h2>

	<% round.game.players.each do |player| %>
		<%= turbo_frame_tag player do %>
			<%= player.name %> - <%= player.score %>
		<% end %>
	<% end %>
<% end %>
